/**
 * Type definitions for recovery configuration
 */

// Configuration value types
export type ConfigValue = string | number | boolean | object | null | undefined;

// Logger argument types
export type LoggerArgs = Array<string | number | boolean | object | Error | null | undefined>;